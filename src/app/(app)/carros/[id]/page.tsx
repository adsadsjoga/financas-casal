import Link from "next/link"; import {notFound} from "next/navigation"; import {ArrowLeft} from "lucide-react"; import {Button} from "@/components/ui/button"; import {requireSession} from "@/lib/auth"; import {createClient} from "@/lib/supabase/server"; import type {Vehicle,VehicleCost,VehicleInstallment,VehicleTransactionLink,Transaction,Account} from "@/lib/database.types"; import {CarroDetalheClient} from "./carro-detalhe-client";
export const metadata={title:"Detalhe do carro ? Finan?as do Casal"}; export default async function CarroPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await requireSession();const db=await createClient();const [v,c,i,l,t,a]=await Promise.all([db.from("vehicles").select("*").eq("id",id).eq("couple_id",s.couple.id).maybeSingle(),db.from("vehicle_costs").select("*").eq("vehicle_id",id).order("occurred_on",{ascending:false}),db.from("vehicle_sale_installments").select("*").eq("vehicle_id",id).order("installment_no"),db.from("vehicle_transaction_links").select("*").eq("vehicle_id",id),db.from("transactions").select("*").eq("couple_id",s.couple.id).neq("type","transferencia").order("occurred_on",{ascending:false}).limit(80),db.from("accounts").select("id,name,currency").eq("couple_id",s.couple.id).eq("archived",false)]);if(!v.data)notFound();return <div className="mx-auto max-w-4xl space-y-5"><Button asChild variant="ghost" size="sm"><Link href="/carros"><ArrowLeft className="size-4"/>Voltar para carros</Link></Button><CarroDetalheClient vehicle={v.data as Vehicle} costs={(c.data??[]) as VehicleCost[]} installments={(i.data??[]) as VehicleInstallment[]} links={(l.data??[]) as VehicleTransactionLink[]} transactions={(t.data??[]) as Transaction[]} accounts={(a.data??[]) as Pick<Account,"id"|"name"|"currency">[]} moeda={s.couple.primary_currency}/></div>}