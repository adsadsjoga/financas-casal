"use client"; import {useState,useTransition} from "react"; import {useRouter} from "next/navigation"; import {CalendarDays,CarFront,Link2,Plus,WalletCards} from "lucide-react"; import {toast} from "sonner"; import {Badge} from "@/components/ui/badge"; import {Button} from "@/components/ui/button"; import {Card,CardContent,CardHeader,CardTitle} from "@/components/ui/card"; import {Input} from "@/components/ui/input"; import {Label} from "@/components/ui/label"; import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from "@/components/ui/select"; import {formatMoney} from "@/lib/money"; import {hojeISO} from "@/lib/dates"; import {adicionarCustoCarro,vincularLancamento} from "../actions"; import type {Vehicle,VehicleCost,VehicleInstallment,VehicleTransactionLink,Transaction,Account} from "@/lib/database.types";
const roles=[['compra','Compra'],['custo','Custo'],['entrada','Entrada'],['parcela','Parcela'],['ajuste','Ajuste']] as const;
export function CarroDetalheClient({vehicle,costs,installments,links,transactions,accounts,moeda}:{vehicle:Vehicle;costs:VehicleCost[];installments:VehicleInstallment[];links:VehicleTransactionLink[];transactions:Transaction[];accounts:Pick<Account,'id'|'name'|'currency'>[];moeda:string}){const router=useRouter();const [pending,start]=useTransition();const [category,setCategory]=useState("Mecânica"),[description,setDescription]=useState(""),[amount,setAmount]=useState(""),[date,setDate]=useState(hojeISO()),[txId,setTxId]=useState(""),[role,setRole]=useState<"compra"|"custo"|"entrada"|"parcela"|"ajuste">("custo");const custo=costs.reduce((s,c)=>s+c.amount_cents,0),total=vehicle.purchase_price_cents+custo,sale=vehicle.sale_price_cents??vehicle.desired_sale_price_cents??0,lucro=sale-total,linked=new Set(links.map(l=>l.transaction_id));function add(e:React.FormEvent){e.preventDefault();start(async()=>{const r=await adicionarCustoCarro({vehicleId:vehicle.id,category,description,amount,occurredOn:date});if(!r.ok){toast.error(r.error);return}toast.success("Custo adicionado.");setAmount("");setDescription("");router.refresh()})}function link(){if(!txId)return;start(async()=>{const r=await vincularLancamento({vehicleId:vehicle.id,transactionId:txId,role});if(!r.ok){toast.error(r.error);return}toast.success("Lançamento vinculado.");setTxId("");router.refresh()})}return <><div className="flex items-start justify-between gap-3"><div className="flex items-start gap-3"><span className="bg-secondary flex size-11 items-center justify-center rounded-lg"><CarFront className="size-6"/></span><div><p className="text-muted-foreground text-xs font-semibold uppercase tracking-[0.12em]">{vehicle.status==="vendido"?"Vendido":"Em estoque"}</p><h1 className="text-2xl font-bold tracking-tight">{vehicle.make} {vehicle.model}</h1><p className="text-muted-foreground text-sm">{vehicle.year??"—"} · {vehicle.color||"Sem cor"}{vehicle.plate?" · "+vehicle.plate:""}</p></div></div><Badge variant={vehicle.status==="vendido"?"secondary":"outline"}>{vehicle.status==="vendido"?"Vendido":"Estoque"}</Badge></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-4">{[["Compra",vehicle.purchase_price_cents],["Custos",custo],["Investido",total],["Lucro",lucro]].map(([label,value])=><Card key={label as string}><CardContent className="p-4"><p className="text-muted-foreground text-xs">{label}</p><p className={"mt-1 font-bold tabular-nums "+(label==="Lucro"&&Number(value)<0?"text-rose-600":"")}>{formatMoney(Number(value),moeda)}</p></CardContent></Card>)}</div><Card><CardHeader><CardTitle className="text-base">Dados da venda</CardTitle></CardHeader><CardContent className="grid gap-3 text-sm sm:grid-cols-3"><div><p className="text-muted-foreground">Preço final/desejado</p><p className="font-medium">{formatMoney(sale,moeda)}</p></div><div><p className="text-muted-foreground">Comprador</p><p className="font-medium">{vehicle.buyer_name||"Ainda não vendido"}</p></div><div><p className="text-muted-foreground">Compra em</p><p className="font-medium">{vehicle.purchase_date}</p></div></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Plus className="size-4"/>Adicionar custo pago</CardTitle></CardHeader><CardContent><form onSubmit={add} className="grid gap-3 sm:grid-cols-2"><div className="space-y-2"><Label>Categoria</Label><Input value={category} onChange={e=>setCategory(e.target.value)}/></div><div className="space-y-2"><Label>Valor</Label><Input required inputMode="decimal" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="500,00"/></div><div className="space-y-2"><Label>Descrição</Label><Input value={description} onChange={e=>setDescription(e.target.value)} placeholder="Mecânico, peças, transporte…"/></div><div className="space-y-2"><Label>Data</Label><Input type="date" value={date} onChange={e=>setDate(e.target.value)}/></div><Button type="submit" disabled={pending} className="sm:col-span-2">Salvar custo</Button></form><div className="mt-5 divide-y border-t">{costs.map(c=><div key={c.id} className="flex justify-between gap-3 py-3 text-sm"><div><p className="font-medium">{c.description||c.category}</p><p className="text-muted-foreground text-xs">{c.category} · {c.occurred_on}</p></div><p className="font-medium tabular-nums">{formatMoney(c.amount_cents,moeda)}</p></div>)}</div></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Link2 className="size-4"/>Conciliar com o Revolut</CardTitle></CardHeader><CardContent className="space-y-3"><p className="text-muted-foreground text-xs">Vincule compras, custos ou recebimentos que já estão em Lançamentos. O vínculo organiza sem duplicar dinheiro.</p><div className="grid gap-3 sm:grid-cols-[1fr_1fr_auto]"><Select value={txId} onValueChange={setTxId}><SelectTrigger><SelectValue placeholder="Escolher lançamento"/></SelectTrigger><SelectContent>{transactions.filter(t=>!linked.has(t.id)).map(t=><SelectItem key={t.id} value={t.id}>{t.occurred_on} · {t.description||"Sem descrição"} · {formatMoney(t.amount_cents,accounts.find(a=>a.id===t.account_id)?.currency??moeda)}</SelectItem>)}</SelectContent></Select><Select value={role} onValueChange={v=>setRole(v as typeof role)}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{roles.map(([v,label])=><SelectItem key={v} value={v}>{label}</SelectItem>)}</SelectContent></Select><Button type="button" onClick={link} disabled={pending||!txId}>Vincular</Button></div>{links.length>0&&<div className="space-y-2 border-t pt-3">{links.map(l=><div key={l.id} className="flex justify-between text-sm"><span className="text-muted-foreground">{roles.find(r=>r[0]===l.role)?.[1]??l.role}</span><Badge variant="secondary">Vinculado</Badge></div>)}</div>}</CardContent></Card>{installments.length>0&&<Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><CalendarDays className="size-4"/>Parcelas a receber</CardTitle></CardHeader><CardContent className="divide-y">{installments.map(i=><div key={i.id} className="flex justify-between py-3 text-sm"><span>Parcela {i.installment_no} · vence {i.due_on}</span><span className="font-medium tabular-nums">{formatMoney(i.amount_cents,moeda)}</span></div>)}</CardContent></Card>}<Card className="border-dashed"><CardContent className="flex items-start gap-3 p-4"><WalletCards className="text-muted-foreground mt-0.5 size-4"/><p className="text-muted-foreground text-xs">Para um pagamento em espécie: Revolut → Dinheiro em mãos; depois registre e vincule o custo deste carro.</p></CardContent></Card></>}
